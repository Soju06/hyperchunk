# hyperchunk

**A bit-exact Minecraft-compatible world generator in pure C.**

Regenerates vanilla Minecraft Java Edition overworld chunks byte-for-byte
identical to the reference implementation, from scratch, with no JVM in the
generation path.

> NOT AN OFFICIAL MINECRAFT PRODUCT. NOT APPROVED BY OR ASSOCIATED WITH MOJANG
> OR MICROSOFT.

---

## Status

**Phase 1 — pre-alpha.** Nothing works yet. The design is locked, the
implementation has not started.

See [DECISIONS.md](DECISIONS.md) for the five architectural decision records
and [.hermes/plans/](.hermes/plans/) for the Phase 1 implementation plan.

## What this is

`hyperchunk` is a from-scratch reimplementation of the vanilla overworld
generation pipeline — noise, surface rules, carvers, features, and lighting —
written in C11 with zero dependencies beyond `libc`.

The correctness bar is absolute: the `sha256` of a generated region file must
match the `sha256` of the same region generated by vanilla Minecraft. Not
"visually similar", not "close enough". Identical bytes.

## Why

Vanilla density functions are evaluated by walking an interpreter tree. Every
node is a megamorphic virtual call, so the JIT cannot inline, and the effective
throughput lands around 0.03–0.08 flops/cycle against a theoretical ceiling of
8 flops/cycle on AVX2 without FMA. That is a 96–256x gap, and it is a data
structure problem rather than a language problem.

The workload is also firmly compute bound. Chunk noise runs at roughly
21.4 flops/byte of arithmetic intensity against a 12-core ridge point near
7.0 flops/byte, which means instruction-level improvements translate directly
into wall-clock time instead of stalling on memory bandwidth.

The catch is that bit-exact parity forbids FMA contraction, since folding
`a*b+c` into a single instruction removes an intermediate rounding step and
silently changes terrain. Losing FMA doubles the number of independent
dependency chains needed to saturate the FP ports, which is why register
pressure — not vector width — is the dominant constraint. See
[ADR-004](DECISIONS.md) for the full analysis.

## Design invariants

These are load-bearing. Violating any of them breaks the project.

| Invariant | Reason |
|---|---|
| Bit-exact parity with vanilla | The entire premise. `sha256` equality is the acceptance test |
| FMA prohibited everywhere | Contraction changes results. Enforced by `-ffp-contract=off` and an `objdump` CI gate |
| FFI boundary is per-region only | Per-node crossings cost 18.5% of chunk time. Node-level entry points are absent from the public header by design |
| Core is a pure compute library | No file I/O, no networking, no world state. Keeps the CLI, JNI, and Rust FFI consumers all viable |
| Unknown extensions fall back to vanilla | Correctness outranks speed. Fallback granularity is the whole chunk, never smaller |

## Architecture

```
┌─ libhyperchunk (pure C11, libc only, C ABI) ───────────────┐
│  arena allocator / SoA block storage                      │
│  flattened density function IR + compiler                 │
│  5-stage pipeline                                         │
│  batch scheduler (checkerboard, avoids feature conflicts)  │
│  API: hyperchunk_batch(seed, region_list, out)  ← region    │
└───────────────────────────────────────────────────────────┘
        ↑                ↑                  ↑
   hyperchunk CLI      JNI (Fabric)      Rust FFI (planned)
   bench + parity   live servers      Pumpkin / Valence
```

Java allocates roughly 40,808 objects per chunk. At 5000 chunk/s that is
6.53 GB/s of allocation pressure against a G1 throughput ceiling near
1–2 GB/s, so larger batches actively hurt. Arena and SoA storage take that to
zero allocations, which inverts the sign: batching gets cheaper as it grows.

## Scope

**In scope for Phase 1**

- Single pinned version: 26.2 (\"Chaos Cubed\")
- Overworld only
- Structures placed but not jigsaw-assembled
- Scalar reference kernel

**Out of scope for Phase 1**

- SIMD kernels (Phase 2)
- Multithreaded batching (Phase 2)
- JNI bridge and Fabric mod (Phase 3)
- Nether and End
- Version matrix support

## Building

Nothing to build yet. Once Phase 1 lands:

```bash
cmake -S . -B build && cmake --build build -j
ctest --test-dir build --output-on-failure
./scripts/check_no_fma.sh        # asserts no vfmadd/vfmsub in the artifact
./scripts/parity_gate.sh         # asserts sha256 equality against vanilla
```

## Prior art

`hyperchunk` occupies a gap rather than competing head-on. Native Minecraft
server implementations consistently stall at world generation:

| Project | What it is | Gap |
|---|---|---|
| [Pumpkin](https://github.com/pumpkin-mc/pumpkin) | Full server in Rust | worldgen and saving incomplete |
| [Valence](https://github.com/valence-rs/valence) | Bevy ECS server framework | no vanilla worldgen |
| [Minestom](https://minestom.net/) | Java framework, no NMS | no vanilla worldgen, by design |
| [Cuberite](https://github.com/cuberite/cuberite) | C++ server | no vanilla parity |
| [C2ME](https://github.com/RelativityMC/C2ME-fabric) | Chunk gen optimization mod | bounded by the JVM |

Reference material that made this tractable:
[cubiomes](https://github.com/Cubitect/cubiomes) for biome and structure
algorithms, and the
[Minecraft Wiki](https://minecraft.wiki/w/Data_pack) for datapack worldgen
schemas.

## License

MIT. See [LICENSE](LICENSE).
